name: Deploy to Koyeb

on:
  push:
    branches:
      - challenge_6

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '20'

    - name: Install dependencies
      run: npm install

    # - name: Build project
    #   run: npm run build

    - name: Deploy to Koyeb
      env:
        KOYEB_API_TOKEN: ${{ secrets.KOYEB_API_TOKEN }}
      run: |
        curl -X POST https://app.koyeb.com/v1/services/6118f01f-01a2-4e2b-bdb5-709aee1dd915/deploy \
        -H "Authorization: Bearer $KOYEB_API_TOKEN" \
        -d '{
              "repository": {
                "url": "https://github.com/naufan17/24001143-synrgy7-nau_bcr-ch6",
                "branch": "challenge_6"
